'use strict';

var React = require('react'),

    Inbox = React.createClass({
      render: function() {
        return (
          <h1 className="Inbox">inbox</h1>
        )
      }
    });

module.exports = Inbox;
